#[Name(test)]

import X
import GL

#[Prototype]
fn Print(str : string)

#[Entry]
fn Main() -> int
{
  i : mut int = 5

  while (i > 0)
  {
    Print("Loop!\n")
    i = i - 1
  }

  return 0
}

operator +(left : int, right : int) -> int
{
}
