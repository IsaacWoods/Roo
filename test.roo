#[Name(test)]

#[Prototype]
fn Print(str : string)

#[Entry]
fn Main(y : int) -> int
{
  left : int = 3
  right : int = 7

  if (left > right)
  {
    Print("Left is greater than right\n")
  }
  else
  {
    Print("Left is not greater than right\n")
  }

  return 0
}
