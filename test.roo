#[Name(test)]

import X
import GL

#[Prototype]
fn Print(str : string)

#[Entry]
fn Main() -> int
{
  i : mut int = 5
  counter : mut int = 0

  while (i > 0)
  {
    counter = counter + 1
    i = i - 1
  }

  return 0
}
