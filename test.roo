#[Name(test)]

import Prelude

type Color
{
  r : uint
  g : uint
  b : uint
}

#[Entry]
fn Main() -> int
{
  color : Color(255, 0, 255)

  if (color.r > 3)
  {
    Print("Hello, World!\n")
  }

  return 0
}
